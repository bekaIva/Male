enum UserType {
  user,
  admin,
}
enum PaymentMethods { CreditCard, ByCash }
enum PaymentStatus {
  NotPaid,
  PENDING,
  PROCESS,
  ON_HOLD,
  COMPLETED,
  CANCELED,
  NOT_FINISHED,
  SAVED,
  PREPARED,
  CLEARED,
  DENIED,
  EXPIRED,
  FAILED,
  REFUNDED,
  DECLINED,
  RETURNED,
  UNKNOWN
}
enum DeliveryStatus { Pending, Accepted, Completed, Canceled }
